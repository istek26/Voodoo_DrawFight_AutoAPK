
name: APK_Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Build Message
        run: |
          echo "Voodoo Prototip APK hazirlaniyor..."
          echo "Bu asama tamamlandiginda 'index.html' icindeki fizik motoru paketlenmis olacak."
